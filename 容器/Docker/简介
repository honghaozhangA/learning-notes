########################################
################ 应用场景 ###############
########################################
1. 简化配置，构建一次可以 打包后既可以用作测试环境优可以做生产环境/预生产环境，可以省去很多测试环节，假如一台服务器 ，可进行多版本的测试，不用排队等待。
